// Account modal was removed in v3 (file + cloud sync, no auth).
// Cross-device transfer is now done via Export / Import save.
// This file is unused; safe to delete.

export function mountAccountModal() {}
